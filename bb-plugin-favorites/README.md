# Favorites

Save a project + machine + harness + model + reasoning + speed setup, then tap it to open New Thread already set to that combo.

The page lives in the sidebar as **Favorites**. Tiles are at the top; the picker stays docked at the bottom. Tapping a tile never starts a thread — it only seeds the composer.
