/**
 * The assistants fleet's home project, as the inbox-sidebar plugin defines it
 * (lib/use-board-state.ts there). Every "assistants" surface in this plugin
 * keys off the project name, matching the Bots section's filter.
 */
export const ASSISTANTS_PROJECT_NAME = "assistants";
